colors = ['красный', 'зеленый', 'синий'];

alert(colors.indexOf('зеленый'));

alert(colors.indexOf('синий'));

alert(colors[2]);

alert(colors.indexOf('фиолетовый'));

alert(colors.indexOf('черный'));
